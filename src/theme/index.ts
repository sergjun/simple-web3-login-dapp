import { extendTheme } from "@chakra-ui/react";

export default extendTheme({
  fonts: {
    heading: "Open Sans",
    body: "Open Sans",
  },

  shadows: {
    outline: "0 0 0 3px #C6F6D5",
  },
});
